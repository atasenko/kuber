apiVersion: v1
kind: Service
metadata:
  name: nginx-mt
spec:
  selector:
    app: nginx-mt
  ports:
    - name: nginx
      protocol: TCP
      port: 80
    - name: multitool
      protocol: TCP
      port: 1180
